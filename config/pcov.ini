; PCOV（PHPUnit 用の高速コードカバレッジドライバ）
; 常時有効だと全リクエストに計測コストが乗るため、既定は無効。
; カバレッジ取得時のみ有効化する:
;   php -d pcov.enabled=1 vendor/bin/phpunit --coverage-text
; もしくは phpunit.xml の <coverage> 設定で pcov を使う。
pcov.enabled = 0
; 計測対象を絞ると高速化する。プロジェクトに合わせて調整（例: /var/www/html）。
; pcov.directory = /var/www/html
